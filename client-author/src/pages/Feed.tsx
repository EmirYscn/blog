import styled from "styled-components";
import Posts from "../ui/Posts";

const StyledFeed = styled.div``;

function Feed() {
  return (
    <StyledFeed>
      <Posts />
    </StyledFeed>
  );
}

export default Feed;
